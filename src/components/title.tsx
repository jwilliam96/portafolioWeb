import { fontRoboto } from "@/ui";

export function Title() {
    return (
        <h2 className={`font-bold text-4xl text-white text-center mb-4 ${fontRoboto.className}`}>
            PROYECTOS
        </h2>
    )
}
